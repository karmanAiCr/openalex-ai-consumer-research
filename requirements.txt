# R Package Requirements
# AI in Consumer Research: OpenAlex Boolean Search
# Supplementary Materials

# To install all packages at once, run in R:
# install.packages(c("openalexR", "dplyr", "tidyr", "stringr", "lubridate", "jsonlite", "httr", "purrr"))

# Core Packages
openalexR >= 1.3.0    # OpenAlex API R interface
dplyr >= 1.1.0        # Data manipulation (part of tidyverse)
tidyr >= 1.3.0        # Data tidying and reshaping
stringr >= 1.5.0      # String manipulation
lubridate >= 1.9.0    # Date and time handling
jsonlite >= 1.8.0     # JSON parsing and generation
httr >= 1.4.0         # HTTP requests and API interactions
purrr >= 1.0.0        # Functional programming tools

# System Requirements
R >= 4.0.0            # Minimum R version

# Optional (for enhanced analysis)
# ggplot2 >= 3.4.0    # Data visualization
# readr >= 2.1.0      # Fast CSV reading/writing
# tibble >= 3.2.0     # Modern data frames

# Notes:
# - All packages are available on CRAN
# - Installation is automatic with install.packages()
# - No development versions required
# - Compatible with Windows, macOS, and Linux

# Installation Instructions:
# 1. Open R or RStudio
# 2. Run: install.packages(c("openalexR", "dplyr", "tidyr", "stringr", "lubridate", "jsonlite", "httr", "purrr"))
# 3. Wait for installation to complete
# 4. Test by running: library(openalexR)

# Troubleshooting:
# - If package installation fails, try: install.packages("package_name", dependencies = TRUE)
# - For system dependency issues on Linux, install: libcurl4-openssl-dev, libssl-dev, libxml2-dev
# - On macOS with M1/M2 chips, ensure you have Rosetta 2 installed if needed
